import { CargaSuaveDirective } from './carga-suave.directive';

describe('CargaSuaveDirective', () => {
  it('should create an instance', () => {
    const directive = new CargaSuaveDirective();
    expect(directive).toBeTruthy();
  });
});
